{
  "additionalProperties": false,
  "description": "Lo que se devuelve al entrar o renovar. SIN refresh token: va en cookie.",
  "properties": {
    "access_token": {
      "title": "Access Token",
      "type": "string"
    },
    "expires_in_seconds": {
      "minimum": 1,
      "title": "Expires In Seconds",
      "type": "integer"
    },
    "token_type": {
      "default": "bearer",
      "title": "Token Type",
      "type": "string"
    },
    "user_id": {
      "title": "User Id",
      "type": "string"
    }
  },
  "required": [
    "access_token",
    "expires_in_seconds",
    "user_id"
  ],
  "title": "SessionResponse",
  "type": "object"
}
